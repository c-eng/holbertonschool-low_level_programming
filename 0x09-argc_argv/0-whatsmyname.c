#include <stdio.h>

/**
 * main - prints program name
 * @argc: argument counter
 * @argv: argument list
 *
 * Return: void
 */

int main(int argc __attribute__((unused)), char *argv[])
{
		printf("%s\n", argv[0]);
		return (0);
}
